import React from 'react';
import { Flex, Text, Heading, useToast, Tag, Grid, GridItem } from '@chakra-ui/react';

import { useSelector, useDispatch } from 'react-redux';

const ProfileLayout = () => {
	return <Flex bg={'gray.100'} h={'70vh'}></Flex>;
};

export default ProfileLayout;
